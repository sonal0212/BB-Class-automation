# from datetime import datetime
# today = datetime.today()
# print(today)
# date_time = today.strftime("%A %H %M %S").split()
# print(date_time)
# curr_day, curr_hour, curr_min, curr_sec = date_time
# print(curr_day, curr_hour, curr_min, curr_sec)
class1 = (9, 40)
test = (1, 2, *class1)
print(test)